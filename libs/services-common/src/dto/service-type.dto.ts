export enum ServiceTypeMethod {
  InClinic = 'InClinic',
  Virtual = 'Virtual',
  PhoneCall = 'PhoneCall',
  External = 'External',
}

export enum ServiceTypeColors {
  InClinic = '#ddf1e4',
  Virtual = '#e9ddf1',
  PhoneCall = '#FAF3E2',
}

export enum ServiceTypeMethodDisplay {
  InClinic = 'In-Clinic',
  Virtual = 'Virtual',
  PhoneCall = 'Phone Call',
}
