export enum MailgunAdapterFunctions {
  CreateMessage = 'CreateMessage',
  Send = 'Send',
}

export enum MailgunAdapterActions {
  AttachmentFailure = 'AttachmentFailure',
  SendError = 'SendError',
}

export enum SendInBlueAdapterFunctions {
  Send = 'Send',
}

export enum SendInBlueAdapterActions {
  SendError = 'SendError',
}
