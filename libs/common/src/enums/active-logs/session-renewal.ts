export enum SessionRenewalInterceptorFunctions {
  Intercept = 'Intercept',
}

export enum SessionRenewalInterceptorActions {
  CheckIdToken = 'CheckIdToken',
  SessionWasNotFound = 'SessionWasNotFound',
}
